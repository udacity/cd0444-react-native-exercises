import { View, Text, Button, TouchableOpacity, Pressable, FlatList, ScrollView, SafeAreaView } from 'react-native'

export default function Views() {

  return (
    <SafeAreaView>
      <ScrollView>
        <Text>These are different kinds of view components</Text>
      </ScrollView>
    </SafeAreaView>
  )
}