import { SafeAreaView, ScrollView } from 'react-native'
import View from '../../features/animals/View'

export default function Welcome() {
  return (
    <SafeAreaView>
      <ScrollView>
        <View />
      </ScrollView>
    </SafeAreaView>
  )
}